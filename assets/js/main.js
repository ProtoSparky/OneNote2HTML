function init(){
    //we try to get the JSON object
    const JsonFileLoc = "./assets/file_index/index.json";
    const FileIndex = ReadJSON(JsonFileLoc, false);
    if(FileIndex =! null){
        //everything Ok, launch rest of code
        
    }
}